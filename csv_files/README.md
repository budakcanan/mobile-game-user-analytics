# Data Files

Place the Kaggle dataset CSV files in this folder with their original names:

- `users.csv`
- `user_sessions.csv`
- `user_purchases.csv`

The CSV files are ignored by Git on purpose, so the repository stays lightweight.
